{
  "name": "mern-stack-authentication",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run install-dependencies && concurrently \"npm run server\" \"npm run client\"",
    "install-dependencies": "cd client && npm install && cd ../server && npm install && cd .. && npm install",
    "server": "node server/server.js",
    "client": "cd client && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
